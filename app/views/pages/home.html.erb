<% if user_signed_in? %>
	<h1>You are logged in!</h1>
<% else %>
	<div class="hero-unit">
		<h1>Home Page</h1>
		<p>This is my first home page in rails!</p>
		<p>
			<%= link_to "Sign Up Now!", new_user_registration_path, class: "btn btn-primary btn-block" %>
		</p>
	</div>
<% end %>